{
  "name": "stock1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.13.0 || >=24.0.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "start:lan": "node --env-file-if-exists=.env scripts/start-lan.mjs",
    "secret": "node scripts/print-secret.mjs",
    "test": "node --test tests/backend/*.test.mjs tests/frontend/*.test.mjs",
    "test:backend": "node --test tests/backend/*.test.mjs",
    "test:frontend": "node --test tests/frontend/*.test.mjs",
    "test:live": "node --test tests/live/*.test.mjs",
    "test:coverage": "node scripts/test-coverage.mjs",
    "test:dates": "node scripts/date-sweep.mjs"
  },
  "dependencies": {
    "fubon-neo": "^2.2.2"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
