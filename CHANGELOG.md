# 更新紀錄

給「已經在用、剛 `git pull` 完」的人看的：這次變了什麼、要不要重啟、有沒有行為改變。

- 動到 `server.mjs` → **重新啟動伺服器**（Node 不會熱載）
- 只動前端 → 瀏覽器 **Ctrl+F5** 一次
- 想確認自己是不是最新版 → 更多 → 版本與更新

完整逐筆歷史請看 `git log --oneline`；這份只記「使用者感覺得到的差別」。

---

## 2026-08-21

**需要：`git pull` → `npm install` → 重啟伺服器 → Ctrl+F5**

### 新增

- **到價提醒現在在背景也會判斷。** 以前分頁切走或視窗最小化就完全不判斷（判斷整條掛在只在前景跑的行情輪詢上）。現在分頁隱藏時會改走背景監看：只抓有提醒的那幾檔、盤中最快 30 秒一輪，到價時發**桌面通知**。第一次設定到價提醒時瀏覽器會問通知權限，也可以到「更多 → 訊號提醒」手動開。
  - 界線沒有變鬆：判斷仍然只吃當日即時價，昨收備援價（`priceStale`）一樣不會觸發。
  - **瀏覽器整個關掉還是收不到**——這個 App 沒有伺服器推播。
- **「更多 → 版本與更新」**：顯示這台實際跑的 commit，並跟 GitHub 上的 `origin` 分支比對，落後時會說落後幾個 commit 並給出更新步驟。三個人各自 `git pull`、畫面對不上時，先來這裡對版。
  - 這是唯一一個會連到 GitHub 的功能（每 6 小時最多一次）。要關掉：`.env` 設 `UPDATE_CHECK=off` 再重啟。
  - 新端點 `GET /api/app-version`；`GET /api/health` 也會一併回報 build commit。
- **`start.bat`**：Windows 雙擊即可啟動（會自動補跑 `npm install` 並開好瀏覽器）。可以「傳送到 → 桌面（建立捷徑）」。
- **`npm run start:lan`**：綁到區域網路，讓同一個 Wi-Fi 下的手機／平板連得進來，並印出手機該輸入的網址。
- **`npm run secret`**：產生對外開放時需要的隨機密鑰。

### 修正

- **`.env` 以前根本沒有被載入。** `.env.example` 存在、README 也寫了環境變數，但程式從來只讀 `process.env`——照著文件把設定寫進 `.env` 的人，設定其實完全沒有生效。現在 `npm start`、`npm run start:lan` 與 `start.bat` 都會載入 `.env`。
- **靜態資產不再每次重傳。** app shell 約 1.2MB，而且每個 request 都重讀檔案原封不動送出。現在加上 ETag（內容沒變回 304）與 gzip（文字類壓縮後約剩 26%）。在自己電腦上感覺不出來，用手機連家裡電腦時差很多。
- **技術圖／zoom 圖的畫布尺寸換算**不再依賴瀏覽器替我們套用「`border-style: none` 時邊框寬度為 0」這條 CSS 規則。真實瀏覽器的行為沒有變，這是一道防呆（jsdom 30 就沒照做，四邊會憑空各扣 16px）。

### 維護

- **不再支援 Node 20**（2026-04-30 已 EOL、不再收安全更新）。需求改為 Node 22.13+／24+，建議 Node 24 LTS。CI 也同步移除 20.x。
- jsdom 29 → 30，清掉 undici 的 5 條 high severity advisory（僅開發相依，執行期一直是 0 漏洞）。
